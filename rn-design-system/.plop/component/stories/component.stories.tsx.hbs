import { ComponentStory, ComponentMeta } from '@storybook/react-native';

import { {{ name }} } from '@rn-design-system/components';

const {{ name }}Meta: ComponentMeta<typeof {{ name }}> = {
  title: '{{ name }}',
  component: {{ name }},
  args: {
    someProp: {{ name }},
  },
};

export default {{ name }}Meta;

type {{ name }}Story = ComponentStory<typeof {{ name }}>;

export const Default: {{ name }}Story = (args) => <{{ name }} {...args} />;

export const Disabled: {{ name }}Story = (args) => <{{ name }} {...args} disabled />;
